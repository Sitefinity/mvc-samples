@model DevMagazine.Issues.Mvc.Models.IIssueModel

<div class="row">
    @foreach (var issue in @Model.Issues)
    {
        @Html.Partial("IssueElement", issue)
    }
</div>

@Html.Partial("EndlessPager", Model)
